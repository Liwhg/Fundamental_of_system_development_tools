[project]
name = "greet-practice"
version = "0.1.0"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project.scripts]
greet-practice = "greet_practice.cli:main"
